package ${packageName}

import android.view.View
import org.jetbrains.anko.*

class ${className}UI : AnkoComponent<${className}Controller> {
    override fun createView(ui: AnkoContext<${className}Controller>): View = with(ui) {

    }
}
