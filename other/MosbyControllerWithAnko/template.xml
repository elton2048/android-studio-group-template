<template format="4"
        revision="1"
        name="Mosby Controller from Conductor with Anko"
        description="Creates the Controller element with Anko component associated to the Controller using Mosby.">

    <category value="Other"/>

    <parameter id="className"
        name="Controller Name"
        type="string"
        constraints="class|unique|nonempty"
        default="Controller"
        help="The name of the controller for views"/>

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />

</template>
